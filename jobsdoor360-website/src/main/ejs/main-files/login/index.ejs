<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Log in</title>
    <link rel="stylesheet" href="/staticfiles/mainfiles/login/style.css" />
    <%- include('../../partials/head.ejs') %>
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css"
    />
    <style>
      .password-container {
        display: flex;
        align-items: center;
      }

      .password-container p {
        margin: 0 0 0 62px;
      }
    </style>
  </head>

  <body>
    <%- include('../../partials/navbar.ejs') %>
    <div class="login_parent">
      <div class="login-container" style="text-align: center">
        <h2 class="login-head">Login to Jobsdoor360</h2>
        <form id="login-form">
          <label for="email">Email:</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="Email"
          />
    
          <label for="password">Password:</label>
          <input type="password" id="password" name="password" required />
    
          <p class="label-right">
            <a class="label" href="/forgotpassword/">Forgot password?</a>
          </p>
    
          <button type="submit" id="login-btn" style="margin-top: 20px">
            Login
          </button>
        </form>
    
        <button
          id="google-login-btn"
          class="google-button"
        >
          <span>Continue with Google</span>
        </button>
    
        <div class="lines-text">
          <span>OR</span>
        </div>
    
        <p class="bottom-heading">
          Don't have an account?
          <span>
            <a href="/signup/">Sign Up</a>
          </span>
        </p>
      </div>
    </div>
    
    
    
    <%- include('../../partials/footer.ejs') %>

    <script
      type="text/javascript"
      src="https://cdn.jsdelivr.net/npm/toastify-js"
    ></script>
    <script type="module" src="/staticfiles/mainfiles/login/script.js"></script>
    <script src="../../../../../../staticfiles/utils.js"></script>
    <script>
      //    async function transferData() {
      // try{
      // step1: Read data from source collection
      //   const sourceSnapshot= await getDocs(collection(db, "users"));
      // step2: Write data to destination collection
      //   sourceSnapshot.forEach(async(doc)=>{
      //     const docData=doc.data();
      //     await setDoc(collection(db,"login_details"), doc.id, docData);
      //      console.log(`Document ${doc.id} transferred successfully.`);
      //   });
      //    console.log("Data transfer completed");
      //  } catch (error){
      //  console.error("Error transferring data:", error);
      // }
      // }

      // call function to start data transfer
      // transferData();
    </script>
  </body>
</html>
